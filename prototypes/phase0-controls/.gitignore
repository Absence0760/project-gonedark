# Godot editor import cache — regenerated on open/import, never commit
.godot/
# Build artifacts (APK lives in out/; *.apk is also ignored repo-wide)
/out/
# .uid files ARE committed (stable resource ids) — do not ignore Main.gd.uid
