# Gradle / AndroidX config for the Going Dark scaffold.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# We ship a NativeActivity-based app; no Kotlin/Java UI yet, but AndroidX is harmless and
# expected by the toolchain.
android.nonTransitiveRClass=true
