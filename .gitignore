# Build output
/build/
/out/
cmake-build-*/
*.o
*.a
*.so
*.apk
*.aab

# Android / Gradle
.gradle/
local.properties
/app/build/
*.iml

# Rust (if the engine goes that route)
/target/

# IDE / editor
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Claude Code — personal/local settings (project settings.json IS checked in)
.claude/settings.local.json

# Env — .env.development IS committed (safe defaults); local/secret variants are not
.env.local
.env*.local
.env.production
.env.prod

# Terraform
infra/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
*.auto.tfvars
crash.log
crash.*.log
override.tf
override.tf.json

# infra-secrets — ONLY KMS-encrypted *.sops.yaml may be committed; block all plaintext
infra-secrets/*
!infra-secrets/.sops.yaml
!infra-secrets/*.sops.yaml
!infra-secrets/*.example.yaml
!infra-secrets/README.md

# Cooked assets (regenerated by the offline pipeline)
/assets/cooked/
*.pak
